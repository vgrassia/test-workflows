---
name: Test

on:
  push:
  workflow_dispatch:

jobs:
  test:
    name: Workflow Testing
    runs-on: ubuntu-22.04
    steps:
    - name: See if Skopeo exists in runner
      run: |
        sudo apt-get update
        sudo apt-get install skopeo
        skopeo --version
